#nginx/nginx.conf
events {
    worker_connections 1024;
}


http {
  server_tokens off;
  server {
    listen 80;
    root  /var/www;


    location / {
      uwsgi_pass uwsgi_server:9090;
      include uwsgi_params;
    }
  }
}